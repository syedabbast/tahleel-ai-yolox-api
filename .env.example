# TAHLEEL.ai GCP Cloud Run Backend - Environment Variable Example
# Copy/rename this file to .env and fill with your actual secrets/config

# --- Google Cloud Storage ---
GCS_BUCKET_NAME=tahleel-ai-videos
GOOGLE_APPLICATION_CREDENTIALS=service-account.json  # Path to service account JSON (for local dev)

# --- YOLOX Model ---
YOLOX_WEIGHTS=yolox_nano.pth
YOLOX_MODEL_NAME=yolox-nano

# --- Flask API ---
PORT=8080

# --- GPT Vision & Claude API Keys ---
GPT_VISION_API_KEY=your-gpt-vision-key
CLAUDE_API_KEY=your-claude-key

# --- Supabase (if used for direct Python integration) ---
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_KEY=your-service-key

# --- Other Config ---
DEBUG=false
